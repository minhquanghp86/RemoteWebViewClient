name: remote_webview
dependencies:
  - espressif/esp_websocket_client
  - espressif/esp-dsp
  - jpegdec
  - espressif/esp_jpeg: "^1.0.0"

  - esp_driver_jpeg:
    require: private
    rules: [ { if: target == "esp32p4" } ]

  - esp_mm:
    require: private
    rules: [ { if: target == "esp32p4" } ]
